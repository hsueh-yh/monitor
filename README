directory structure:

./mtlib
Source files of the mtlib library, a NDN-based streaming data transmission development library.

./Qshell
Source files of the video playout shell, which dependes on the mtlib library.

./doc
Using documents for mtlib and Qshell

./statistic
Log analysis script files

./bin
library and executable file directory, if exist
